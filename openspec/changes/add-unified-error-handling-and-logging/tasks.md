## 1. Implementation
- [x] 1.1 定义错误码：错误码枚举/常量与对外语义（稳定字符串）
- [x] 1.2 定义标准错误响应结构（含 traceId/timestamp/path，可选字段级 errors）
- [x] 1.3 实现全局异常处理（`@ControllerAdvice`）：覆盖参数校验、协议解析、显式状态码异常与兜底异常
- [x] 1.4 手工验证：400/415/500 等场景返回结构与 `code` 正确，且不泄漏敏感信息
- [x] 1.5 实现 traceId 组件：解析/生成 `X-Request-Id`，写入 MDC，并在响应头与错误响应回传
- [x] 1.6 实现关键链路日志：记录入参摘要、耗时、状态码，并可通过 traceId 关联
- [x] 1.7 实现错误日志：记录 traceId + 关键上下文 + 异常栈（避免敏感信息）
- [x] 1.8 调整日志输出格式：确保 console 日志包含 traceId
- [x] 1.9 手工验证：成功与错误路径的日志链路完整可定位问题

